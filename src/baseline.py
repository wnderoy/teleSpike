"""baseline.py — STUB.

Planned: estimate each word's usual arrival rate (msgs/min) per (channel, word,
hour-of-day) from saved history, floored at a minimum so rare words still get a
working drain. Writes the map the queueburst engine consumes.
"""
