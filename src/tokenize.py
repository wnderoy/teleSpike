"""tokenize.py — STUB.

Planned: split a message into lowercased, stopword-free word tokens. Shared by
the streaming job, the baseline batch job and the replay harness so every count
in the pipeline comes from the same token definition.
"""
