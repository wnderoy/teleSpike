"""scan_telegram.py — STUB (Telegram scraper).

Planned: Telethon-based scraper that pulls public-group history, saves each
channel as data/history/<channel>.jsonl ({channel, sender, text, ts}), and
resumes incrementally via a .meta.json offset. Saved history doubles as the
baseline warm-up.
"""
